import { FormEvent, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { GraduationCap } from "lucide-react";
import Button from "../components/Button";
export default function Login(){const navigate=useNavigate();const [identifier,setIdentifier]=useState("");const [password,setPassword]=useState("");const [remember,setRemember]=useState(false);const [submitting,setSubmitting]=useState(false);const handleSubmit=(e:FormEvent)=>{e.preventDefault();setSubmitting(true);setTimeout(()=>navigate("/dashboard"),600)};return <div className="flex min-h-[80vh] items-center justify-center bg-bg px-5 py-16"><div className="w-full max-w-sm"><div className="mb-8 text-center"><span className="mx-auto flex h-12 w-12 items-center justify-center rounded-xl bg-brand-blue text-white"><GraduationCap size={22}/></span><h1 className="mt-4 text-2xl font-bold text-brand-navy">Welcome back.</h1><p className="mt-1 text-sm text-subink">Sign in to manage your hostel bookings.</p></div><form onSubmit={handleSubmit} className="rounded-2xl border border-slate-200 bg-white p-6"><label htmlFor="identifier" className="text-sm font-medium text-ink">Student Email / Registration Number</label><input id="identifier" type="text" required value={identifier} onChange={e=>setIdentifier(e.target.value)} placeholder="you@students.mmust.ac.ke" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3.5 py-2.5 text-sm focus:border-brand-blue focus:outline-none focus:ring-1 focus:ring-brand-blue"/><label htmlFor="password" className="mt-4 block text-sm font-medium text-ink">Password</label><input id="password" type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••" className="mt-1.5 w-full rounded-lg border border-slate-200 px-3.5 py-2.5 text-sm focus:border-brand-blue focus:outline-none focus:ring-1 focus:ring-brand-blue"/><div className="mt-4 flex items-center justify-between text-sm"><label className="flex items-center gap-2 text-ink"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)} className="h-4 w-4 rounded border-slate-300 accent-brand-blue"/>Remember me</label><a href="#" className="font-medium text-brand-blue hover:underline">Forgot password?</a></div><Button type="submit" fullWidth size="lg" className="mt-6" disabled={submitting}>{submitting?"Signing in…":"Sign In"}</Button><Link to="/dashboard"><Button type="button" variant="outline" fullWidth size="lg" className="mt-3">Continue as Guest</Button></Link></form><p className="mt-2 text-center text-xs text-subink">This is a demo sign-in — no real account is created.</p></div></div>}
